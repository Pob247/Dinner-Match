{
  "name": "what-do-you-want-for-dinner",
  "version": "1.0.0",
  "description": "Family meal planner - runs on your local network so everyone can connect",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/server.js",
    "dev": "npm run build && node dist/server.js"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/express": "^5.0.6",
    "@types/node": "^25.0.2",
    "typescript": "^5.9.3"
  }
}
